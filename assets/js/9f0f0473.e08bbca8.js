"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["411"],{6986(e,t,a){a.r(t),a.d(t,{metadata:()=>o,default:()=>u,frontMatter:()=>r,contentTitle:()=>c,toc:()=>h,assets:()=>l});var o=JSON.parse('{"id":"m9-serving-llm-workloads/quiz","title":"Quiz: Serving AI & LLM Workloads at Scale","description":"<Quiz questions={[","source":"@site/docs/m9-serving-llm-workloads/quiz.mdx","sourceDirName":"m9-serving-llm-workloads","slug":"/m9-serving-llm-workloads/quiz","permalink":"/304-kubeadv/docs/m9-serving-llm-workloads/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":5,"frontMatter":{"sidebar_position":5,"title":"Quiz: Serving AI & LLM Workloads at Scale"},"sidebar":"courseSidebar","previous":{"title":"Deep Dive","permalink":"/304-kubeadv/docs/m9-serving-llm-workloads/deep-dive"},"next":{"title":"Lesson: Agentic Kubernetes, Platform for and by AI","permalink":"/304-kubeadv/docs/m10-agentic-kubernetes/lesson"}}'),n=a(4848),s=a(8453),i=a(7517);let r={sidebar_position:5,title:"Quiz: Serving AI & LLM Workloads at Scale"},c="Serving AI & LLM Workloads at Scale \u2014 Quiz",l={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,s.R)(),...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.header,{children:(0,n.jsx)(t.h1,{id:"serving-ai--llm-workloads-at-scale--quiz",children:"Serving AI & LLM Workloads at Scale \u2014 Quiz"})}),"\n",(0,n.jsx)(i.A,{questions:[{prompt:"Your team deploys an LLM behind a Kubernetes Service. At 50 concurrent users, responses time out \u2014 but kubectl top shows the GPU at 40% utilization. What is the most likely cause?",options:[{text:"GPU memory is exhausted by KV caches \u2014 every active request holds key-value attention pairs, and 50 concurrent requests filled the available GPU memory before compute became a bottleneck",correct:!0,explanation:"GPU compute utilization is misleading for LLM inference. The decode forward pass is small (low FLOPS), so compute stays low. But every active request holds a KV cache that grows with sequence length. GPU memory fills up with caches long before GPU cores are saturated. New requests cannot start because there is no memory for their cache \u2014 not because there are no compute cycles."},{text:"The CPU is bottlenecking the GPU \u2014 tokenization and data preprocessing are overwhelming the host",correct:!1,explanation:"Tokenization is fast and rarely the bottleneck for inference. The timeout pattern with low GPU utilization points to memory pressure (KV cache), not CPU preprocessing."},{text:"Round-robin load balancing is sending too many requests to one replica",correct:!1,explanation:"Load imbalance would show high GPU utilization on one replica, not low utilization across all. The 40% utilization across the board with timeouts is the classic KV cache exhaustion pattern."},{text:"The model weights are too large for the GPU \u2014 they should be quantized",correct:!1,explanation:"If the model weights did not fit, the pod would fail to start entirely. The fact that it handles 1-10 users fine means the weights are loaded. The scaling issue is dynamic memory (KV cache per request), not static memory (model weights)."}]},{prompt:"An LLM generates a 200-token response. The user sees the first token after 150ms (TTFT), then each subsequent token arrives every 25ms (ITL). What determines the TTFT?",options:[{text:"The prefill phase \u2014 the model reads and encodes the entire input prompt in parallel before producing the first output token. TTFT scales with prompt length.",correct:!0,explanation:"Prefill processes all prompt tokens in one forward pass to build the initial KV cache. A 100-token prompt is faster than a 10,000-token prompt. After prefill, decode generates one token per forward pass at roughly constant ITL. TTFT is dominated by prefill compute; ITL is dominated by memory bandwidth for reading the KV cache."},{text:"Network latency between the client and the serving pod \u2014 the model generates all tokens instantly",correct:!1,explanation:"Network latency adds a fixed overhead (typically < 10ms in-cluster), not the 150ms observed. The TTFT is dominated by the prefill computation, which processes the entire prompt to build the KV cache before the first token can be generated."},{text:"The time to load model weights from storage into GPU memory",correct:!1,explanation:"Model weights are loaded once at pod startup (30-120 seconds for a 7B model). After startup, weights stay in GPU memory. TTFT is a per-request latency, not a startup cost."},{text:"Queue waiting time \u2014 the request waited 150ms for a processing slot",correct:!1,explanation:"Queue wait time adds to TTFT but does not define it. Even with zero queue depth, TTFT is non-zero because the prefill computation takes time proportional to prompt length. The 150ms here is compute, not queue."}]},{prompt:"PagedAttention (from the vLLM project) applies virtual memory concepts to the KV cache. What is the primary problem it solves?",options:[{text:"Pre-allocation waste \u2014 without PagedAttention, each request pre-allocates a contiguous memory block for the maximum possible sequence length, wasting 60-80% of GPU memory on unused space",correct:!0,explanation:"The early approach allocated max-sequence-length blocks upfront. A request that might generate 2048 tokens gets a 2048-token cache block even if it only uses 50. PagedAttention allocates fixed-size pages on demand, achieving near-zero internal fragmentation. The same GPU serves 2-4x more concurrent requests."},{text:"Model weight distribution \u2014 PagedAttention splits weights across multiple GPUs",correct:!1,explanation:"Weight distribution is tensor parallelism, a separate technique. PagedAttention manages the KV cache (per-request attention state), not the model weights (shared across all requests)."},{text:"Network transfer overhead \u2014 PagedAttention compresses KV data for faster inter-node transfer",correct:!1,explanation:"PagedAttention is about GPU memory management, not network transfer. It divides cache into pages allocated on demand, like an OS page table. Compression and transfer are separate concerns (relevant to disaggregated serving)."},{text:"Slow token generation \u2014 PagedAttention parallelizes the autoregressive decode loop",correct:!1,explanation:"Autoregressive generation is inherently sequential \u2014 each token depends on all previous tokens. PagedAttention does not change this. It improves memory efficiency so more requests can run concurrently, increasing throughput (not per-request speed)."}]},{prompt:"Which signals should you use for autoscaling an LLM serving Deployment? Select all that apply.",multiSelect:!0,options:[{text:"Queue depth \u2014 requests waiting for a KV cache processing slot. If the queue is non-zero for sustained periods, you need more replicas.",correct:!0,explanation:"Queue depth is the most practical starting signal. A growing queue means demand exceeds capacity. It is directly actionable: queue > 0 sustained \u2192 scale up."},{text:"KV cache utilization \u2014 fraction of GPU memory used by active request caches. Above 80% means the next burst will cause queuing.",correct:!0,explanation:"KV cache utilization is the most predictive signal \u2014 it is a leading indicator. By the time queue depth rises, users are already waiting. Cache at 80% means you are one burst away from queuing."},{text:"CPU utilization \u2014 the standard HPA metric for scaling Deployments",correct:!1,explanation:"CPU utilization is irrelevant for GPU inference. The serving engine runs on the GPU; CPU handles only tokenization and HTTP serving, which are never the bottleneck. Scaling on CPU would never fire for memory-bound inference."},{text:"TTFT (time to first token) at P95 \u2014 if prefill latency crosses the SLA threshold, the user experience has already degraded",correct:!0,explanation:"TTFT is the most user-visible signal. If P95 TTFT exceeds your SLA (e.g., 500ms), users notice. It is a lagging indicator (the problem already happened), but it is the right signal for SLA-based scaling policies."}]},{prompt:"The Gateway API Inference Extension uses InferencePool and InferenceModel CRDs. How does its routing differ from a standard Kubernetes Service?",options:[{text:"The endpoint-picker queries per-replica metrics (KV cache occupancy, queue depth, active requests) and routes each request to the replica with the most free capacity \u2014 not round-robin",correct:!0,explanation:"A standard Service uses iptables/ipvs for round-robin or random selection with no visibility into backend state. The Inference Extension endpoint-picker is a gRPC service that checks each replica health and selects based on cache utilization and queue depth. A replica at 85% cache gets no new traffic until others catch up."},{text:"It uses DNS-based load balancing instead of iptables, which is faster for gRPC",correct:!1,explanation:"The extension uses an endpoint-picker gRPC sidecar, not DNS. The improvement is not transport-layer speed but routing intelligence \u2014 model-aware, state-aware selection instead of blind distribution."},{text:"It replaces the kube-proxy entirely and handles all cluster traffic",correct:!1,explanation:"The Inference Extension only handles inference traffic routed through the Gateway. Standard Services and kube-proxy continue to handle all other cluster traffic."},{text:"It schedules pods onto nodes based on GPU availability, replacing the kube-scheduler for inference workloads",correct:!1,explanation:"The Inference Extension is a routing layer, not a scheduling layer. It decides which existing pod gets each request. The kube-scheduler (with DRA) handles pod placement. These are different concerns: placement vs. request routing."}]},{prompt:"Disaggregated serving separates prefill from decode onto different GPUs. Why does this improve throughput compared to running both on the same GPU?",options:[{text:"Prefill is compute-bound (parallel matrix multiplication) while decode is memory-bandwidth-bound (sequential KV cache reads). Running both on one GPU creates interference \u2014 a large prefill steals compute from decode, causing ITL spikes for all active requests.",correct:!0,explanation:"Monolithic serving forces both phases to share the same GPU resources. A new request prefill is a large matrix-matrix multiply that saturates compute, starving in-progress decode steps (small matrix-vector multiplies that need memory bandwidth). Separation lets prefill GPUs maximize FLOPS and decode GPUs maximize bandwidth, independently."},{text:"It doubles the total GPU memory available, so more KV cache can be stored",correct:!1,explanation:"Disaggregation does not simply double memory \u2014 the KV cache must be transferred from the prefill GPU to the decode GPU. The benefit is eliminating compute interference between phases, not increasing memory capacity."},{text:"It allows using CPUs for decode instead of GPUs, reducing hardware cost",correct:!1,explanation:"Decode still requires GPU (or specialized accelerator) for the forward pass. The separation is between compute-optimized GPUs (high FLOPS for prefill) and memory-optimized GPUs (high bandwidth for decode), not GPU vs CPU."},{text:"Network transfer of KV cache is faster than computing it locally",correct:!1,explanation:"Network transfer adds latency (10-20ms for InfiniBand). The benefit is not faster KV cache access but eliminating the interference between prefill compute and decode memory access on the same GPU."}]},{prompt:"A serving engine runs continuous batching. A batch of 8 requests is in flight, and request 3 finishes generating after only 50 tokens while the other 7 keep going. What happens next that would NOT happen under traditional (static) batching?",options:[{text:"The next waiting request immediately takes request 3's freed slot \u2014 the GPU keeps processing a full batch instead of sitting idle with requests still queued",correct:!0,explanation:"Continuous batching re-evaluates the active batch at every decode step. A finished request is removed and its freed blocks let the scheduler admit the next waiting request right away. There is no static batch boundary, so a short response does not force the GPU to wait for the longest request in the group."},{text:"The finished request's KV cache blocks stay reserved until all 8 original requests complete, in case it needs to resume",correct:!1,explanation:"A finished request (hit EOS or max length) has its blocks freed immediately, not held. Holding blocks for a completed request would waste memory for no benefit \u2014 the request has nothing left to generate."},{text:"All 8 requests restart their forward pass from token 0 so the batch stays synchronized",correct:!1,explanation:"Restarting would throw away every request's KV cache and redo the compute already done \u2014 the opposite of what continuous batching is for. Each request's progress is independent; only the batch membership changes per step."},{text:"The GPU pauses decoding until a full new batch of 8 requests has accumulated in the queue",correct:!1,explanation:"That describes static batching, which continuous batching replaces specifically to avoid this kind of pause. Under continuous batching, the engine fills the freed slot with whatever is next in the queue, even if that is a single request."}]},{prompt:"An A100 running Llama-3-8B at 4096-token context can serve roughly 115 concurrent requests. One user then submits a single request with a 128K-token context. What is the operational impact on the other users on that GPU?",options:[{text:"That one request consumes roughly 32\xd7 the KV cache memory of a 4096-token request, so it can evict or block dozens of short-context sessions on its own",correct:!0,explanation:"KV cache size scales linearly with sequence length. A context 32\xd7 longer uses roughly 32\xd7 the memory (about 17 GB vs. 537 MB for this model), so one long-context request can consume the memory budget that would otherwise serve dozens of typical requests \u2014 this is why prompt length, not just request count, drives capacity planning."},{text:"There is no impact \u2014 KV cache size depends only on the model, not on how long the prompt or context is",correct:!1,explanation:"The opposite is true. KV cache size is proportional to sequence length (per-request cache = layers \xd7 heads \xd7 head-dim \xd7 sequence-length \xd7 bytes). Longer context means proportionally more memory, independent of the model staying fixed."},{text:"It doubles the GPU's compute usage but leaves memory usage unchanged",correct:!1,explanation:"A longer context grows the KV cache (memory), not compute in the same way \u2014 decode still does one forward pass per token regardless of how much cache it reads. The dominant cost of a long context is memory, not FLOPS."},{text:"The request is automatically rejected by Kubernetes because it exceeds a cluster-wide context length limit",correct:!1,explanation:"Kubernetes has no concept of prompt length or context window \u2014 that is entirely managed by the serving engine, and only if you have explicitly configured a max-context limit. Without one, the request is accepted and simply consumes whatever memory it needs."}]},{prompt:"You configure a KServe InferenceService for a 70B model with scale-to-zero enabled and no minReplicas set. Traffic stops for ten minutes, then one request arrives. What should you expect, and how would you avoid this for a latency-sensitive production endpoint?",options:[{text:"A cold start of roughly 2-5 minutes while the Deployment scales up from zero and the model reloads into GPU memory; set minReplicas: 1 (or pre-cache weights on local storage) to avoid this for production traffic",correct:!0,explanation:"Scale-to-zero tears down the Deployment entirely, so the next request triggers the full loading pipeline again \u2014 downloading or reading weights, deserializing, transferring to GPU, and warming up. For a 70B model this takes minutes, which is why production, latency-sensitive endpoints keep at least one replica warm."},{text:"No delay \u2014 Knative keeps the model weights resident in GPU memory even when replicas scale to zero",correct:!1,explanation:"Scaling to zero replicas means the Pod (and its GPU memory) is gone. Nothing is kept warm. The Knative activator intercepts the incoming request and triggers a fresh scale-up, which includes reloading the model from scratch."},{text:"The request fails permanently because there are no replicas available to serve it",correct:!1,explanation:"This is exactly what the Knative activator exists to prevent \u2014 it holds the request, triggers the scale-up to one replica, and forwards the request once the Pod is ready, rather than failing it."},{text:"The cold start only affects container image pull time; model loading happens in the background before the container is considered started",correct:!1,explanation:"Model loading is not a background step \u2014 the readiness probe does not pass until the model is loaded and warmed up, so the request genuinely waits for the full pipeline: image pull, weight download/read, deserialization, GPU transfer, and warmup."}]},{prompt:"Speculative decoding uses a small draft model to propose several candidate tokens, which the main model then verifies in a single forward pass. What actually determines whether this technique speeds up generation?",options:[{text:"The acceptance rate \u2014 how many of the draft model's candidate tokens the main model confirms match its own output. At 60-80% acceptance you get roughly 1.5-2\xd7 throughput; a low acceptance rate wastes the draft pass",correct:!0,explanation:"The verification pass costs about the same as generating one token normally, but if most of the K draft tokens are accepted, you got several tokens for that one pass. If the draft model's guesses are usually wrong, you paid for the draft pass and the verification pass and gained little \u2014 acceptance rate is the deciding variable."},{text:"The size of the main model \u2014 larger main models always see bigger speedups from speculative decoding regardless of the draft model's accuracy",correct:!1,explanation:"Main model size affects the cost of each verification pass, but the speedup itself comes from how often the draft model's guesses are correct. A large main model paired with a poorly matched draft model still gets a low acceptance rate and little benefit."},{text:"Whether the draft model runs on a physically separate GPU from the main model",correct:!1,explanation:"Speculative decoding does not require a separate GPU \u2014 the draft model typically adds a modest GPU memory overhead (roughly 2-4 GB for a 1B draft model) alongside the main model. GPU placement is not what determines the speedup."},{text:"The KV cache page size configured for PagedAttention",correct:!1,explanation:"Page size affects memory fragmentation and allocation granularity for the KV cache, not the token-acceptance behavior of speculative decoding. These are independent optimizations that happen to both live inside the serving engine."}]},{prompt:"Which of the following genuinely reduce cold-start latency for a large LLM serving pod? Select all that apply.",multiSelect:!0,options:[{text:"Tensor parallelism \u2014 splitting model weights across multiple GPUs so each one only has to load a fraction of the total weights",correct:!0,explanation:"Loading a 70B model across 4 GPUs means each GPU loads roughly 35 GB instead of 140 GB, cutting the weight-transfer phase of loading time by close to 4\xd7. The tradeoff is that all GPUs must be allocated and ready before loading can start."},{text:"Weight caching on local NVMe \u2014 mounting pre-downloaded model files so the pod skips the network download phase entirely",correct:!0,explanation:"The weight-download phase (10-30s for a 7B model, 60-180s for a 70B model) is one of the largest contributors to cold start. Reading from local NVMe instead of pulling from S3/GCS eliminates that phase, at the cost of needing a local copy of the weights on every node that might run the model."},{text:"Pre-warming \u2014 running a warmup forward pass before the readiness probe is allowed to pass, so CUDA kernel compilation does not happen on the first real request",correct:!0,explanation:"vLLM and SGLang run a warmup inference on startup specifically so kernel compilation is paid for before traffic arrives. The readiness probe only succeeds once this warmup finishes, so the first real user never absorbs that cost."},{text:"Raising the HPA's CPU utilization scaling target so the Deployment scales down less aggressively",correct:!1,explanation:"CPU utilization is irrelevant to GPU inference scaling \u2014 the serving engine's bottleneck is GPU memory and compute, not CPU. Changing a CPU-based HPA target does nothing to shorten the model-loading pipeline that dominates cold start."}]},{prompt:"You quantize the KV cache from fp16 to int8 for a Llama-3-8B deployment. What is the actual effect of this change?",options:[{text:"It roughly halves the per-token KV cache memory (about 131 KB down to 65 KB), which roughly doubles how many concurrent requests the same GPU memory can hold",correct:!0,explanation:"KV cache quantization shrinks the bytes-per-element term in the cache-size calculation. Halving that term halves the memory each request's cache consumes, so a fixed memory budget can now hold roughly twice as many concurrent requests \u2014 the accuracy impact is typically small because KV cache values tolerate quantization better than model weights do."},{text:"It halves the memory used by the model weights instead of the KV cache",correct:!1,explanation:"KV cache quantization and weight quantization are separate techniques targeting separate memory pools. Setting a flag like `--kv-cache-dtype` changes only how the attention cache is stored; the model weights are quantized (if at all) through a different mechanism."},{text:"It doubles GPU compute throughput during the decode phase",correct:!1,explanation:"Quantizing the KV cache is a memory-capacity optimization, not a compute optimization \u2014 decode is memory-bandwidth-bound, so smaller cache entries can modestly help bandwidth, but the documented and primary benefit is more concurrent requests fitting in memory, not doubled compute throughput."},{text:"It has no measurable effect on either accuracy or capacity \u2014 it is a purely cosmetic setting",correct:!1,explanation:"Quantizing the KV cache directly changes how much GPU memory each request's cache occupies, which directly changes how many requests fit concurrently. There is a real (if usually small) accuracy tradeoff, and a real, measurable capacity gain."}]},{prompt:"An `InferenceModel` resource defines two `targetModels` entries: `llama-3-v1` at weight 90 and `llama-3-v2` at weight 10. What does this configuration achieve, and how do you respond if v2's metrics come back worse than v1's?",options:[{text:"It splits traffic 90/10 between the two model versions for a canary rollout; if v2 shows worse TTFT or a higher error rate, you adjust the weights (for example back to 100/0) without redeploying anything",correct:!0,explanation:"The Gateway handles the weighted split transparently \u2014 the backend pods are unaware of it, and the client only ever requests the shared `modelName`. Because the split is just a weight, rolling back a bad canary is a configuration change, not a redeploy."},{text:"It runs both model versions on every request and returns whichever one responds first",correct:!1,explanation:"That would double GPU cost for every request and is not how weighted traffic splitting works. Each individual request is routed to exactly one target model, chosen according to the configured weights."},{text:"It requires the client to specify which model version it wants in the request path or headers",correct:!1,explanation:"Clients request the model by its user-facing `modelName` only; the weight-based split to a specific version happens inside the gateway and is invisible to the client. This is what makes canary rollouts possible without any client-side changes."},{text:"It keeps v2 as a cold standby that only receives traffic if v1 fails health checks",correct:!1,explanation:"That describes failover, not weighted traffic splitting. With `weight: 90` and `weight: 10`, v2 actively receives roughly 10% of live traffic all the time, not just when v1 is unhealthy."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>i});var o=a(4848),n=a(6540);function s({q:e,state:t,onToggle:a}){return(0,o.jsxs)("div",{className:"question_QpiT",children:[(0,o.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,o.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,o.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,n)=>{let s=t.selected.has(n),i="option_qGol";return t.checked?e.correct?i+=" correct_LYA5":s&&(i+=" incorrect_kRFc"):s&&(i+=" chosen_H3WG"),(0,o.jsxs)("li",{children:[(0,o.jsxs)("button",{type:"button",className:i,disabled:t.checked,onClick:()=>a(n),children:[(0,o.jsx)("span",{className:"marker_vN_w",children:s?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(s||e.correct)&&e.explanation&&(0,o.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},n)})})]})}function i({questions:e}){let[t,a]=(0,n.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),c=t.filter((t,a)=>{var o,n;let s;return o=e[a],n=t.selected,s=o.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),o.multiSelect?n.size===s.length&&[...n].every(e=>o.options[e].correct):1===n.size&&s.includes([...n][0])}).length;return(0,o.jsxs)("div",{className:"quiz_I014",children:[e.map((n,i)=>(0,o.jsx)(s,{q:n,state:t[i],onToggle:t=>a(a=>a.map((a,o)=>{if(o!==i)return a;let n=new Set(a.selected);return e[i].multiSelect?n.has(t)?n.delete(t):n.add(t):(n.clear(),n.add(t)),{...a,selected:n}}))},i)),(0,o.jsxs)("div",{className:"actions_XUwt",children:[(0,o.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,o.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,o.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>i,x:()=>r});var o=a(6540);let n={},s=o.createContext(n);function i(e){let t=o.useContext(s);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(n):e.components||n:i(e.components),o.createElement(s.Provider,{value:t},e.children)}}}]);