"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["6985"],{3357(e,t,o){o.r(t),o.d(t,{metadata:()=>s,default:()=>u,frontMatter:()=>i,contentTitle:()=>c,toc:()=>d,assets:()=>l});var s=JSON.parse('{"id":"m10-agentic-kubernetes/quiz","title":"Quiz: Agentic Kubernetes \u2014 Platform for and by AI","description":"<Quiz questions={[","source":"@site/docs/m10-agentic-kubernetes/quiz.mdx","sourceDirName":"m10-agentic-kubernetes","slug":"/m10-agentic-kubernetes/quiz","permalink":"/304-kubeadv/docs/m10-agentic-kubernetes/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":5,"frontMatter":{"sidebar_position":5,"title":"Quiz: Agentic Kubernetes \u2014 Platform for and by AI"},"sidebar":"courseSidebar","previous":{"title":"Deep Dive","permalink":"/304-kubeadv/docs/m10-agentic-kubernetes/deep-dive"},"next":{"title":"Lesson: Capstone \u2014 Production War-Room","permalink":"/304-kubeadv/docs/m11-capstone-war-room/lesson"}}'),a=o(4848),n=o(8453),r=o(7517);let i={sidebar_position:5,title:"Quiz: Agentic Kubernetes \u2014 Platform for and by AI"},c="Agentic Kubernetes \u2014 Platform for and by AI \u2014 Quiz",l={},d=[];function h(e){let t={h1:"h1",header:"header",...(0,n.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(t.header,{children:(0,a.jsx)(t.h1,{id:"agentic-kubernetes--platform-for-and-by-ai--quiz",children:"Agentic Kubernetes \u2014 Platform for and by AI \u2014 Quiz"})}),"\n",(0,a.jsx)(r.A,{questions:[{prompt:"In the M10 lab, the MCP tool server exposes both read tools (list_pods) and write tools (create_pod). RBAC enforcement happens at the Kubernetes API server, not inside the MCP server. Why is this the correct architecture?",options:[{text:"Enforcing at the API server means the boundary cannot be bypassed \u2014 a compromised tool server, a different client, or a direct API call all hit the same RBAC check. It also produces audit evidence for every attempt.",correct:!0,explanation:"RBAC is enforced by the API server on every request, regardless of which client sent it. If the MCP server filtered tools itself, a bug or compromise could expose write operations. API-level enforcement is the only boundary that cannot be circumvented by the tool server code. The audit log records both allowed and denied attempts, providing forensic evidence."},{text:"The MCP server does not know which ServiceAccount it runs as, so it cannot check permissions",correct:!1,explanation:"The MCP server could read its own ServiceAccount token and call the SelfSubjectAccessReview API to check permissions. The reason enforcement belongs at the API server is not capability \u2014 it is that API-level enforcement is the only unforgeable boundary. Application-level checks can be bypassed."},{text:"The MCP protocol specification requires servers to expose all tools regardless of permissions",correct:!1,explanation:"The MCP spec does not require exposing all tools. A server could filter tools. The architectural reason to let the API server enforce is security \u2014 the API server is the trust boundary, and its audit log captures every denied attempt as evidence."},{text:"Checking permissions in the MCP server would be too slow because it requires an extra API call per request",correct:!1,explanation:"A SelfSubjectAccessReview call adds ~1ms latency \u2014 negligible. The reason is not performance but security architecture: the API server is the single enforcement point that no client can bypass, and it provides audit evidence automatically."}]},{prompt:"An AI agent receives an alert that a pod is crashlooping. It reads the pod logs, checks recent events, and correlates the timing with a recent deployment rollout. Which stage of the agent control loop is this?",options:[{text:"Diagnose \u2014 the agent is reading cluster state and correlating evidence to understand the root cause",correct:!0,explanation:"Diagnose is the stage where the agent reads cluster state (get pods, describe nodes, read logs, list events) and correlates evidence. The agent already detected the problem (the alert). It has not yet proposed a fix or executed anything. All operations in this stage are read-only."},{text:"Detect \u2014 the agent is monitoring signals and receiving alerts",correct:!1,explanation:"Detect is the stage where the agent first becomes aware of the problem \u2014 watching events, reading metrics, receiving webhook alerts. The agent has already moved past detection (it received the alert). Reading logs and correlating with deployments is diagnosis, not detection."},{text:"Propose \u2014 the agent is drafting a remediation plan",correct:!1,explanation:"Propose is the stage where the agent generates a YAML diff or describes planned changes. The agent is still gathering evidence (reading logs, checking events). It has not yet formed a remediation proposal."},{text:"Verify \u2014 the agent is confirming a fix and checking for regression",correct:!1,explanation:"Verify happens after execution \u2014 the agent checks whether the fix worked. The agent has not executed anything yet. It is still in the investigation phase, reading state to understand what went wrong."}]},{prompt:"In the MCP protocol, which JSON-RPC method does an agent client call to discover what tools a server offers?",options:[{text:"tools/list \u2014 returns an array of tool objects with name, description, and inputSchema",correct:!0,explanation:"The MCP protocol defines tools/list as the discovery method. The server responds with its full tool catalog, including the JSON Schema for each tool input. The agent uses this to select the right tool and construct valid arguments. In the lab, this returned 7 tools (5 read, 2 write)."},{text:"initialize \u2014 the handshake that sets up the session",correct:!1,explanation:"Initialize is the session handshake where client and server exchange protocol versions and capabilities. It tells the client that the server supports tools, but does not list them. The client must call tools/list separately to discover the actual tools."},{text:"tools/call \u2014 the method that invokes a specific tool",correct:!1,explanation:"tools/call invokes a specific tool by name with arguments. The agent must already know the tool exists (from tools/list) before calling it. tools/call is execution, not discovery."},{text:"resources/list \u2014 the method that lists available resources",correct:!1,explanation:"resources/list is a different MCP capability for listing data resources (files, database tables). Tools and resources are separate concepts in MCP. Tool discovery uses tools/list."}]},{prompt:"Which of the following Kubernetes operations should be classified as Tier 3 (never delegated to an AI agent)? Select all that apply.",multiSelect:!0,options:[{text:"Delete a namespace \u2014 destroys all resources inside it, including PersistentVolumeClaims and running workloads",correct:!0,explanation:"Deleting a namespace is irreversible and has unbounded blast radius \u2014 every resource in the namespace is destroyed. Data stored in PVCs within the namespace is lost. This should never be delegated to an autonomous agent."},{text:"Delete PersistentVolumeClaims \u2014 causes permanent data loss if the reclaim policy is Delete",correct:!0,explanation:"PVC deletion with a Delete reclaim policy permanently destroys the underlying storage volume. This is irreversible data loss. Even with Retain policy, the PV becomes unbound and requires manual intervention to recover."},{text:"Edit a ClusterRole \u2014 could escalate the agent own privileges or grant cluster-admin to other identities",correct:!0,explanation:"Modifying RBAC roles is a privilege escalation vector. An agent that can edit ClusterRoles could grant itself create/delete permissions, bypassing the read-only boundary. RBAC modifications should always require human execution and review."},{text:"Scale a Deployment from 3 to 5 replicas \u2014 a reversible operation that changes workload capacity",correct:!1,explanation:"Scaling is reversible (scale back to 3) and has bounded blast radius (only affects the target Deployment). This is Tier 2: approval-gated. A human should be aware and approve, but the agent does not need to be the one executing."}]},{prompt:"In the lab, you inspected a raw audit event for a denied create_pod attempt. Which pieces of information does the Kubernetes audit log capture for this denied request?",options:[{text:"The full identity chain: ServiceAccount name, namespace, groups, the pod that made the request, source IP, user agent, the verb attempted, the target resource, and the HTTP response code (403)",correct:!0,explanation:"The audit event captures the complete forensic picture: user identity (system:serviceaccount:m10-lab:ai-agent), the pod making the request (mcp-server-xxx via authentication.kubernetes.io/pod-name), source IP (172.18.0.x), user agent (Python-urllib/3.12), verb (create), target resource (pods in m10-lab), and response code (403). This is enough to trace exactly who tried what and from where."},{text:"Only the ServiceAccount name and whether the request was allowed or denied \u2014 no details about the resource or source",correct:!1,explanation:"The audit log at Metadata level captures much more than user identity and outcome. It includes the full request URI, verb, target resource and namespace, source IPs, user agent, and the response status code. The lab demonstrated all of these fields in the raw audit event."},{text:"The request body (the pod manifest the agent tried to create) and the response body",correct:!1,explanation:"At Metadata audit level (the core-internals default for most resources), request and response bodies are NOT captured \u2014 only metadata about the request. The Request level captures request bodies, and RequestResponse captures both. The lab cluster audit policy uses Metadata for most resources to keep volume manageable."},{text:"The name of the RBAC role that denied the request and the specific rule that was evaluated",correct:!1,explanation:"The audit log records that the request was denied (403 Forbidden) but does not include which ClusterRole or rule was evaluated. To determine why a request was denied, you use kubectl auth can-i or SelfSubjectAccessReview \u2014 the audit log tells you what happened, not the RBAC evaluation path."}]},{prompt:"Why should AI agents start with read-only Kubernetes access by default, rather than being given the permissions they will eventually need?",options:[{text:"Five of six agent control loop stages are read-only. Agents operate at machine speed and will use every permission they have \u2014 a misconfigured agent with delete access will delete things when its reasoning concludes deletion is the fix.",correct:!0,explanation:"The agent control loop (detect, diagnose, propose, approve, execute, verify) only needs write access for the execute stage, and even that should be gated by human approval. Unlike humans, agents have no judgment about what not to do \u2014 they will exercise every permission at machine speed. Starting read-only and adding permissions incrementally, with justification, is the principle of least privilege applied to a user that never exercises restraint."},{text:"Kubernetes RBAC does not support granting write permissions to ServiceAccounts",correct:!1,explanation:"RBAC fully supports granting any verb (create, delete, patch, update) to any ServiceAccount. The question is not capability but governance \u2014 whether you should grant those permissions to an automated agent without careful scoping and approval gates."},{text:"Read-only access is faster because the API server does not need to check authorization for read requests",correct:!1,explanation:"The API server checks authorization for every request, read or write. There is no performance difference. The reason for read-only defaults is security: limiting blast radius, not improving performance."},{text:"AI models cannot generate valid Kubernetes YAML, so write operations would always fail anyway",correct:!1,explanation:"Modern AI models can generate valid Kubernetes YAML reliably. The concern is not capability but intent \u2014 the model may generate correct YAML for a destructive action (like deleting a namespace) that it believes is the right fix. Read-only access prevents the damage regardless of YAML validity."}]},{prompt:"Kubernetes 1.22 replaced legacy ServiceAccount tokens with bound tokens (via the TokenRequest API) as the default. What is the key security gap that legacy tokens had, which bound tokens close?",options:[{text:"Legacy tokens never expired and were not scoped to a specific audience or pod \u2014 if extracted from a compromised pod, they stayed valid indefinitely and could be replayed against other services",correct:!0,explanation:"Legacy tokens were long-lived Secret objects with no expiry, no audience restriction, and no binding to the pod that requested them. Bound tokens fix all three: they expire (default 1 hour), are scoped to a specific audience, and become invalid the moment the pod that requested them is deleted."},{text:"Legacy tokens were not automatically mounted into pods, so ServiceAccounts had to authenticate manually",correct:!1,explanation:"Legacy tokens were also auto-mounted into pods via a projected Secret volume, just like bound tokens are today. Automatic mounting was never the difference \u2014 the gap was in expiry, audience scoping, and pod binding."},{text:"Legacy tokens were encrypted at rest, while bound tokens are stored as plaintext JWTs, making bound tokens less secure",correct:!1,explanation:"Both legacy and bound tokens are JWTs \u2014 base64-encoded, not encrypted. This was never a distinguishing property. Decoding either type reveals its claims in plain text; the security improvement is expiry and scoping, not encryption."},{text:"Legacy tokens could only authenticate to the Kubernetes API server, while bound tokens can authenticate to any external service",correct:!1,explanation:"This is backwards. Audience scoping on bound tokens restricts them to a declared audience (typically the API server) \u2014 it narrows where a token is valid, it does not broaden it. Legacy tokens, lacking any audience restriction, were the ones more exposed to being replayed elsewhere."}]},{prompt:"An AI agent ServiceAccount has only get and list permissions on pods. Why can it not create a RoleBinding that grants create and delete permissions to itself or another ServiceAccount?",options:[{text:"RBAC has a built-in escalation guard: you cannot grant permissions you do not already hold yourself, unless you have been separately granted the escalate verb on roles or clusterroles",correct:!0,explanation:"This is a deliberate Kubernetes RBAC safeguard. Even if the agent has permission to create RoleBindings, the API server refuses to let it bind a role containing permissions the agent does not already possess. The only way around this guard is the explicit escalate verb, which should never be granted to an agent ServiceAccount."},{text:"ServiceAccounts are never permitted to create RoleBinding objects, regardless of their RBAC rules",correct:!1,explanation:"ServiceAccounts can be granted create permission on rolebindings like any other resource. The restriction is not on creating the object \u2014 it is the escalation guard that blocks binding a role whose permissions exceed what the creator already has."},{text:"Only users authenticated via a kubeconfig certificate can create RoleBindings \u2014 ServiceAccount tokens are rejected by the RBAC API",correct:!1,explanation:"The RBAC API treats ServiceAccounts and certificate-authenticated users identically for authorization purposes. There is no special restriction based on authentication method. The escalation guard applies to any subject, not just ServiceAccounts."},{text:"The bound token expires before a two-step privilege escalation (create RoleBinding, then use the new permissions) can complete",correct:!1,explanation:"Token expiry (default 1 hour) is unrelated to this guard \u2014 the escalation attempt is blocked instantly at creation time by the API server, not by a race against token expiry. Even with an hour of validity, the RoleBinding creation itself would be rejected."}]},{prompt:"An agent ClusterRole grants get and list on the pods resource, with no other rules. Can the agent use this to exec into a container with kubectl exec?",options:[{text:"No \u2014 pods/exec is a distinct subresource from pods. Granting access to pods does not grant access to pods/exec, which must be listed explicitly and is a Tier 3 operation",correct:!0,explanation:"Kubernetes treats pods/exec, pods/log, and pods/portforward as separate resources from pods in RBAC rules, even though they share a URL prefix. A rule for pods only covers get/list/watch on pod objects and metadata \u2014 it says nothing about exec. Exec access must be granted with its own explicit rule, and given what it allows (arbitrary command execution inside a container), it belongs on the never-delegate list."},{text:"Yes \u2014 subresources automatically inherit the verbs granted on their parent resource",correct:!1,explanation:'RBAC does not cascade permissions from a resource to its subresources. pods/exec, pods/log, and pods/portforward each require their own explicit rule with resources: ["pods/exec"] (etc.) even if the ClusterRole already grants get/list/watch on pods.'},{text:"Yes, but only for pods the agent itself created, due to ownership-based access control",correct:!1,explanation:'Kubernetes RBAC has no concept of ownership-based access \u2014 permissions are evaluated purely from the ClusterRole/Role rules bound to the subject, not from who created a resource. There is no RBAC mechanism that treats "resources I created" differently from any other matching resource.'},{text:"No, but only because ClusterRoles are cluster-scoped and pods/exec requires a namespace-scoped Role instead",correct:!1,explanation:"ClusterRoles can absolutely grant namespaced-resource permissions, including pods/exec, when bound with a RoleBinding or ClusterRoleBinding \u2014 scope is not the issue here. The real reason exec is blocked is that it is a separate resource that was never listed in the rule at all."}]},{prompt:"A sandboxed process spawned by an agent ignores SIGTERM and keeps running past its activeDeadlineSeconds timeout. What actually stops it?",options:[{text:"The kubelet enforces activeDeadlineSeconds independently of the container process \u2014 it kills the pod when the deadline passes, regardless of whether the process handles SIGTERM gracefully",correct:!0,explanation:"activeDeadlineSeconds is enforced by the kubelet itself, not by cooperation from the process inside the container. Even a runaway process that ignores SIGTERM gets killed when the deadline expires, because the kubelet terminates the pod at the infrastructure level rather than asking the process to exit voluntarily."},{text:"gVisor Sentry detects that the timeout has elapsed and terminates the intercepted syscalls from within its user-space kernel",correct:!1,explanation:"gVisor intercepts and restricts system calls for isolation, but it does not track or enforce pod-level timeouts \u2014 that is the kubelet's job via activeDeadlineSeconds. gVisor and the timeout mechanism address different problems: syscall isolation versus wall-clock enforcement."},{text:"The deny-all NetworkPolicy starves the process of network access, causing it to exit once it cannot reach any external endpoint",correct:!1,explanation:"A CPU-bound or purely local process does not need network access to keep running, so a NetworkPolicy has no effect on whether it terminates. NetworkPolicy prevents data exfiltration and outbound connections \u2014 it does not enforce a runtime deadline."},{text:"The ResourceQuota in the sandbox namespace begins rejecting further system calls once the quota is exceeded, forcing the process to halt",correct:!1,explanation:"ResourceQuota is an admission-time control \u2014 it blocks the creation of new objects (like additional pods) that would exceed the quota. It does not monitor or interrupt syscalls made by an already-running process, and it has nothing to do with wall-clock timeouts."}]},{prompt:"An organization chooses GitOps-based change proposals (the agent commits a fix to a repo, a human reviews the PR, Argo CD applies it) instead of a synchronous admission-webhook approval gate. What trade-off did they accept?",options:[{text:"Slower time-to-remediation \u2014 minutes instead of seconds \u2014 in exchange for a complete audit trail in Git, peer review before the change lands, and an easy rollback path via a simple revert",correct:!0,explanation:"GitOps-based remediation adds latency because the change waits in a PR for review and for the GitOps controller to reconcile, rather than applying immediately. In return, the organization gets Git history as a permanent audit trail, a human review step baked into the workflow, and rollback that is as simple as reverting the commit."},{text:"GitOps changes bypass Kubernetes RBAC entirely, since Argo CD or Flux applies them with unrestricted cluster-admin access",correct:!1,explanation:"Argo CD and Flux authenticate to the cluster with their own ServiceAccount, which is still subject to RBAC like any other identity. GitOps does not grant unrestricted access \u2014 it just moves the human review step earlier, into the pull request, rather than gating the live API call."},{text:"The agent needs direct write access to the Kubernetes API to push its proposed change into the GitOps repository",correct:!1,explanation:"This is exactly what GitOps avoids. The agent commits to a Git repository, which requires only Git credentials, not Kubernetes API write access. The GitOps controller (Argo CD or Flux), not the agent, is the identity that actually applies changes to the cluster."},{text:"Admission webhooks provide a more complete audit trail than Git history, so the organization sacrificed traceability for speed",correct:!1,explanation:"This has the trade-off backwards. Git history \u2014 commits, PR discussion, reviewer approvals \u2014 is generally a richer, more permanent audit trail than an admission webhook decision, which typically only records an allow/deny outcome. The organization gained traceability with GitOps, not lost it."}]},{prompt:"Three agents (A, B, C) all call the same shared kubectl-mcp tool server, which runs under a single read-only ServiceAccount. Agent A also happens to have its own ServiceAccount with additional write permissions granted directly against the Kubernetes API. When Agent A calls a tool on kubectl-mcp, whose permissions apply to the resulting Kubernetes API request?",options:[{text:"The tool server's own ServiceAccount permissions apply \u2014 the MCP server authenticates to the API server as itself, so Agent A gets only the read-only access of kubectl-mcp through this path, regardless of what Agent A's own ServiceAccount can do directly",correct:!0,explanation:'The MCP server holds its own identity and its own bound token, separate from the agent that calls it. When it executes a tool, it calls the Kubernetes API as itself \u2014 not as an impersonation of the caller. This is why the lesson separates "the agent\'s SA controls what the agent can do directly" from "the tool server\'s SA controls what the agent can do through tools": the two are independent boundaries, and a shared tool server gives every caller the same access, no more.'},{text:"Agent A's broader write permissions apply, because MCP forwards the caller's identity to the Kubernetes API via an impersonation header",correct:!1,explanation:"The lab's MCP server does not perform identity impersonation \u2014 it authenticates to the Kubernetes API with its own mounted ServiceAccount token, not a token or header representing the calling agent. Without an explicit impersonation design, the caller's broader permissions never reach the API server."},{text:"The union of Agent A's permissions and the tool server's permissions applies, giving Agent A the greater of the two access levels",correct:!1,explanation:"RBAC permissions are evaluated per-identity for the request actually sent to the API server \u2014 there is no mechanism that merges the permissions of two different ServiceAccounts into a combined set for a single call. Only the identity that makes the request (the tool server) matters here."},{text:"Kubernetes rejects the call outright, because a caller with broader permissions elsewhere is not allowed to invoke a more restricted shared tool server",correct:!1,explanation:"There is no RBAC rule that compares a caller's permissions elsewhere in the cluster against the permissions of the resource it is calling. The tool server simply executes with its own identity; Agent A's unrelated permissions on other resources are irrelevant to this call."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,n.R)(),...e.components};return t?(0,a.jsx)(t,{...e,children:(0,a.jsx)(h,{...e})}):h(e)}},7517(e,t,o){o.d(t,{A:()=>r});var s=o(4848),a=o(6540);function n({q:e,state:t,onToggle:o}){return(0,s.jsxs)("div",{className:"question_QpiT",children:[(0,s.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,s.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,s.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,a)=>{let n=t.selected.has(a),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":n&&(r+=" incorrect_kRFc"):n&&(r+=" chosen_H3WG"),(0,s.jsxs)("li",{children:[(0,s.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>o(a),children:[(0,s.jsx)("span",{className:"marker_vN_w",children:n?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(n||e.correct)&&e.explanation&&(0,s.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},a)})})]})}function r({questions:e}){let[t,o]=(0,a.useState)(e.map(()=>({selected:new Set,checked:!1}))),i=t.every(e=>e.checked),c=t.filter((t,o)=>{var s,a;let n;return s=e[o],a=t.selected,n=s.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),s.multiSelect?a.size===n.length&&[...a].every(e=>s.options[e].correct):1===a.size&&n.includes([...a][0])}).length;return(0,s.jsxs)("div",{className:"quiz_I014",children:[e.map((a,r)=>(0,s.jsx)(n,{q:a,state:t[r],onToggle:t=>o(o=>o.map((o,s)=>{if(s!==r)return o;let a=new Set(o.selected);return e[r].multiSelect?a.has(t)?a.delete(t):a.add(t):(a.clear(),a.add(t)),{...o,selected:a}}))},r)),(0,s.jsxs)("div",{className:"actions_XUwt",children:[(0,s.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>o(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,s.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>o(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),i&&(0,s.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,o){o.d(t,{R:()=>r,x:()=>i});var s=o(6540);let a={},n=s.createContext(a);function r(e){let t=s.useContext(n);return s.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function i(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:r(e.components),s.createElement(n.Provider,{value:t},e.children)}}}]);