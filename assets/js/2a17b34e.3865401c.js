"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["2482"],{1801(e,t,r){r.r(t),r.d(t,{metadata:()=>a,default:()=>p,frontMatter:()=>i,contentTitle:()=>c,toc:()=>h,assets:()=>l});var a=JSON.parse('{"id":"m5-crds-operators/quiz","title":"Quiz: Extending Kubernetes \u2014 CRDs & Operators","description":"<Quiz questions={[","source":"@site/docs/m5-crds-operators/quiz.mdx","sourceDirName":"m5-crds-operators","slug":"/m5-crds-operators/quiz","permalink":"/304-kubeadv/docs/m5-crds-operators/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Extending Kubernetes \u2014 CRDs & Operators"},"sidebar":"courseSidebar","previous":{"title":"Deep Dive","permalink":"/304-kubeadv/docs/m5-crds-operators/deep-dive"},"next":{"title":"Lesson: Kubernetes at Scale: OpenAI and GKE Engineering Lessons","permalink":"/304-kubeadv/docs/m6-kubernetes-at-scale/lesson"}}'),o=r(4848),s=r(8453),n=r(7517);let i={sidebar_position:3,title:"Quiz: Extending Kubernetes \u2014 CRDs & Operators"},c="Extending Kubernetes \u2014 CRDs & Operators \u2014 Quiz",l={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,s.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"extending-kubernetes--crds--operators--quiz",children:"Extending Kubernetes \u2014 CRDs & Operators \u2014 Quiz"})}),"\n",(0,o.jsx)(n.A,{questions:[{prompt:"You install the Website CRD, then create a Website that asks for two replicas. No operator is running. What happens?",options:[{text:"Nothing runs \u2014 the Website is stored in etcd with printer columns and defaults, but there is no Deployment, Service, or Pod",correct:!0,explanation:"This is the spine of the module. A CRD only teaches the API server a new schema. The object is valid and stored, but a CRD has no behaviour \u2014 a register nobody reads. Behaviour comes from a controller, which you have not deployed yet."},{text:"The API server creates a Deployment with two replicas automatically",correct:!1,explanation:"The API server stores and validates the object; it does not act on custom kinds. There is no built-in logic that knows what a Website is supposed to do."},{text:"The create is rejected because no controller is registered for the kind",correct:!1,explanation:"The create succeeds \u2014 a CRD does not require a controller to accept objects. It is stored inert. The absence of a controller is exactly why nothing then happens."},{text:"The Website goes Pending until an operator schedules it",correct:!1,explanation:"There is no Pending state for a custom resource and nothing schedules it. It simply sits in etcd as data until a controller reads it."}]},{prompt:"Which statements about an operator and the spec/status split are correct? Select every one that applies.",multiSelect:!0,options:[{text:"The operator is a reconcile loop: it compares the desired spec with actual cluster state and acts to close the gap",correct:!0,explanation:"Right. It is the same level-triggered loop from M1, now watching your custom kind \u2014 observe, diff, act, on every pass."},{text:"The user owns spec (what they want); the operator owns status (what is actually true)",correct:!0,explanation:"Right. This is the same split as every built-in kind. You set replicas in spec; the operator writes Ready and readyReplicas into status. Neither clobbers the other."},{text:"It is level-triggered \u2014 delete a child it created and it recreates it on the next pass, without being told",correct:!0,explanation:"Right. It acts on the gap between desired and actual, not on events. That is why it self-heals drift."},{text:"The operator writes the spec and the user writes the status",correct:!1,explanation:"Backwards. Users declare intent in spec; controllers report reality in status. If users wrote status, it would not reflect what actually happened."},{text:"Once the operator creates the children, it stops watching and never runs again",correct:!1,explanation:"It is a continuous loop. If it stopped, deleted children would never be recreated and the cluster would drift \u2014 which is exactly what happens when the operator is scaled to zero."}]},{prompt:"A user reports a Website has been stuck in Terminating for an hour after they deleted it. What is the most likely cause?",options:[{text:"A finalizer is on the object and the controller that should remove it is not running (crashed, scaled to zero, or uninstalled)",correct:!0,explanation:"This is the classic stuck-deletion. The finalizer holds the object until its controller does cleanup and removes it. No healthy controller means nothing removes the finalizer, so the object waits in Terminating indefinitely."},{text:"etcd is out of space and cannot delete the object",correct:!1,explanation:"That would surface as write failures across the whole cluster, not one object stuck with a deletionTimestamp. The Terminating state with a finalizer present points squarely at the finalizer mechanism."},{text:"The kubectl delete command failed and never reached the API server",correct:!1,explanation:"The object has a deletionTimestamp, which means the delete request was accepted and recorded. The API server is deliberately holding the object because of the finalizer \u2014 the request did not fail."},{text:"The Website has too many replicas to delete quickly",correct:!1,explanation:"Deletion is not throttled by replica count, and replicas belong to the child Deployment, not the Website. The blocker is the finalizer, not the size of anything."}]},{prompt:"You delete a Website while its operator is switched off (and its finalizer has been removed). Its child Deployment and Service disappear anyway. What removed them?",options:[{text:"The garbage collector, honouring the ownerReferences the operator stamped on the children",correct:!0,explanation:"Right. ownerReferences are parentage on record. The garbage collector \u2014 a controller built into the API server \u2014 reaps owned children when the parent is deleted, entirely independent of whether the operator is running."},{text:"The operator ran one last cleanup before the object was removed",correct:!1,explanation:"The operator was off, so it did nothing. The cleanup here is the API server\u2019s own garbage collector acting on ownerReferences, which is the point of isolating the operator in this test."},{text:"The kubelet deleted the pods when it noticed the Website was gone",correct:!1,explanation:"The kubelet manages pods on its node from their PodSpec; it does not watch custom resources or delete Deployments and Services. Cascading deletion by ownership is the garbage collector\u2019s job."},{text:"Nothing removed them \u2014 they must have been deleted by hand",correct:!1,explanation:"They were removed automatically. With a controller ownerReference pointing at the parent, deleting the parent triggers garbage collection of the children."}]},{prompt:"The CRD carries a structural schema and a CEL rule. Which of these bad specs are rejected by the API server at admission, before any operator is involved? Select all that apply.",multiSelect:!0,options:[{text:"replicas: 9 \u2014 rejected by the CEL rule self.replicas <= 5",correct:!0,explanation:"Right. CEL validation runs in-process in the API server at admission. No webhook, no operator \u2014 the guardrail is enforced before the object is stored."},{text:"host omitted \u2014 rejected because the schema marks host as required",correct:!0,explanation:"Right. Required-field validation is part of the structural schema and runs at admission. The message reads spec.host: Required value."},{text:"a field the schema never declared (e.g. tls: true) \u2014 rejected by strict field validation",correct:!0,explanation:"Right. Modern kubectl sends objects for strict validation, so an undeclared field is rejected as an unknown field rather than silently pruned."},{text:"replicas: 3 with a valid host \u2014 rejected because the operator has not approved it",correct:!1,explanation:"This is a perfectly valid spec and is accepted. Admission validation is about the schema and CEL, not operator approval \u2014 the operator acts after the object is stored, not as a gatekeeper for creation."},{text:"Any spec is accepted; the operator does all validation later",correct:!1,explanation:"The opposite is true here. The API server enforces the schema and CEL at admission, before storage. Pushing validation entirely to the operator would let invalid objects into etcd."}]},{prompt:"With the operator scaled to zero, you delete a child Deployment it created; it stays gone. You scale the operator back to one and the Deployment reappears. Which principle does this demonstrate?",options:[{text:"Level-triggered reconciliation \u2014 the operator drives actual state toward the desired spec on every pass, so it re-converges once it is running again",correct:!0,explanation:"Right. The operator does not react to the delete event; it notices, on its next reconcile, that reality no longer matches the Website spec and recreates the child. While it is off, nothing converges \u2014 that is drift."},{text:"Edge-triggered handling \u2014 the operator queued the delete event and replayed it on restart",correct:!1,explanation:"It is the opposite of edge-triggered. The operator did not remember the delete; it simply compared desired to actual on its next pass and closed the gap. No event needed to be stored or replayed."},{text:"The API server automatically recreates objects owned by a Website",correct:!1,explanation:"The API server garbage-collects children when a parent is deleted; it does not recreate them. Recreation is the operator\u2019s reconcile, which only happens when the operator is running."},{text:"Finalizers recreated the Deployment when the operator restarted",correct:!1,explanation:"Finalizers block deletion of an object; they do not create resources. The child came back because the operator reconciled the gap, not because of any finalizer."}]},{prompt:'You apply a Website with `--validate=false` and a field the schema never declared, such as `secretSauce: "top-secret"`. What does the API server do with that field?',options:[{text:"It silently prunes (drops) the field \u2014 a structural schema only persists fields it declares, so anything outside the schema never reaches etcd",correct:!0,explanation:"Right. Pruning is what makes a CRD safe to store: the API server never keeps a field it cannot account for. This is a separate mechanism from strict client-side validation, which is what normally turns a typo into a loud error."},{text:"It rejects the request with an unknown-field error, the same as a normal kubectl apply",correct:!1,explanation:"That error comes from kubectl\u2019s client-side strict validation, which `--validate=false` deliberately bypasses. Once the request reaches the server, an undeclared field is pruned, not rejected."},{text:"It stores the field as-is, since the structural schema only checks declared fields, not undeclared ones",correct:!1,explanation:"A structural schema governs the whole object, not just the fields it lists. Anything outside the declared structure is dropped before storage, never persisted alongside it."},{text:"It rejects the request because the CEL rule flags the extra field",correct:!1,explanation:"CEL rules evaluate expressions over declared fields, like `self.replicas <= 5`. They do not detect or reject undeclared fields \u2014 pruning handles that, and it happens quietly, not as a rejection."}]},{prompt:'A CRD reports `v1alpha1 served=true storage=true` and `status.storedVersions=["v1alpha1"]`. What do `served` and `storage` actually control?',options:[{text:"`served` decides whether the API server answers requests for that version; `storage` marks the single version objects are actually persisted as in etcd",correct:!0,explanation:"Right. You can serve several versions of a kind at once, but only one of them is ever the storage version \u2014 the schema used to write objects physically in etcd."},{text:"`served` marks which version is written to etcd; `storage` decides which versions clients are allowed to request",correct:!1,explanation:"This has the two flags backwards. Serving is about answering client requests; storage is about which schema is persisted. They are independent \u2014 a version can be served without being the storage version."},{text:"Every version on the CRD must have both `served: true` and `storage: true` at the same time",correct:!1,explanation:"Only one version at a time can carry `storage: true`. You can serve multiple versions simultaneously, but exactly one of them is the storage version."},{text:"`status.storedVersions` lists which versions can currently be requested by clients",correct:!1,explanation:"That is what `served` is for. `storedVersions` records every version that has ever been the storage version \u2014 it matters when retiring an old version, since you cannot drop one still listed there until every stored object has been rewritten."}]},{prompt:"You add a `v1beta1` to the Website CRD that renames `host` to `hostname`, while objects are still stored as `v1alpha1`. A client requests the object as `v1beta1`. What has to happen for that request to follow the correct schema?",options:[{text:"The CRD needs `strategy: Webhook` and a conversion webhook that translates the stored `v1alpha1` object into the `v1beta1` schema on the fly",correct:!0,explanation:"Right. Once versions genuinely use different schemas, the default `None` conversion strategy is not enough \u2014 a conversion webhook is what bridges the stored version and the requested version for every read and write."},{text:"Nothing extra \u2014 the default `None` conversion strategy relabels the apiVersion and handles any schema difference automatically",correct:!1,explanation:"`None` only works when versions are structurally compatible. Renaming a field is exactly the kind of change it cannot bridge \u2014 that is what forces a conversion webhook."},{text:"The client must always request the storage version directly; the CRD cannot serve a different schema from the one stored",correct:!1,explanation:"That defeats the point of serving multiple versions. Clients are meant to be able to request a version different from the storage version, and conversion is precisely the mechanism that makes that work."},{text:"You would need two separate CRDs, one per version",correct:!1,explanation:"A single CRD is built to serve multiple versions of one kind through `spec.versions` \u2014 that is the whole mechanism this question is about, not a reason to split into separate CRDs."}]},{prompt:"About admission webhooks and CEL validation, which statements are correct? Select every one that applies.",multiSelect:!0,options:[{text:"Mutating webhooks run first in admission and can rewrite the object \u2014 for example injecting a sidecar container or setting a default",correct:!0,explanation:"Right. Mutating webhooks run before schema validation and before validating webhooks, and they are allowed to change the object on the way in."},{text:"CEL rules run in-process inside the API server, so they need no webhook service deployed and cannot become a point of failure the way a slow or broken webhook can",correct:!0,explanation:"Right. This is exactly why the lab enforced `replicas <= 5` with CEL: no extra service to run, keep available, or worry about wedging object creation cluster-wide."},{text:"Validating webhooks run after schema validation and can only accept or reject the object, not rewrite it",correct:!0,explanation:"Right. By the time validating webhooks run, mutation is already done and the object has passed schema checks \u2014 validating webhooks only get a yes/no vote."},{text:"CEL can express any validation a webhook can, including rules that need to check other objects in the cluster",correct:!1,explanation:"CEL rules only see the object being validated (`self`). Consulting other objects or external state is exactly the case that still requires a webhook."},{text:"The fixed admission order is validating webhooks first, then mutating webhooks, then schema validation",correct:!1,explanation:"This reverses the real order. Mutating webhooks run first, then schema validation, then validating webhooks \u2014 mutation always happens before anything is judged final."}]},{prompt:"A downstream system polls the Website object as its signal that cleanup is complete, so the parent must stay visible until every child it owns is actually gone. Which `--cascade` policy do you delete it with?",options:[{text:"`foreground` \u2014 it holds the parent in a deleting state via a `foregroundDeletion` finalizer until every child is gone, then removes the parent last",correct:!0,explanation:"Right. Foreground deletion is slower precisely because it waits, which is what gives a downstream watcher a trustworthy signal: the parent never disappears while its children still exist."},{text:"`background`, the default \u2014 it deletes the parent immediately and lets the garbage collector reap the children afterward",correct:!1,explanation:"Background is fast, but for a moment the parent is gone while children are still being cleaned up. A downstream system watching for the parent to vanish would fire its signal too early."},{text:"`orphan` \u2014 it deletes the parent and leaves the children\u2019s `ownerReferences` intact",correct:!1,explanation:"Orphan does the opposite of intact: it severs the children\u2019s ownerReferences and leaves them running, undeleted. Nothing about cleanup ever completes under this policy."},{text:"No cascade flag is needed \u2014 plain `kubectl delete` already waits for children first",correct:!1,explanation:"Plain `kubectl delete` uses the `background` default, which does not wait for children. Waiting for children first is specifically what `foreground` adds."}]},{prompt:"`kubectl get apiservices` shows most entries with `SERVICE Local`, including `v1.apiextensions.k8s.io` \u2014 the group that serves your CRDs. One entry, `v1beta1.metrics.k8s.io`, points at a real Service backing a pod instead. What does that difference tell you about when to reach for an aggregated API server instead of a CRD?",options:[{text:"`Local` means the kube-apiserver serves the group itself (true for both built-ins and CRDs); a real `SERVICE` reference means a separate API server process backs it \u2014 reach for aggregation only when you need custom storage, a non-etcd backend, or logic a schema cannot express",correct:!0,explanation:"Right. A CRD is served by the kube-apiserver and stored in etcd with no server code written. Aggregation means running your own API server process \u2014 worth it only when a CRD\u2019s schema-and-etcd model genuinely does not fit."},{text:"`Local` means the API group is deprecated and scheduled for removal",correct:!1,explanation:"`Local` is not a deprecation marker. It simply means the group is served directly by the kube-apiserver process rather than proxied out to a separate backing service."},{text:"Every CRD you create eventually shows up with a real `SERVICE` reference once it has enough custom resources",correct:!1,explanation:"CRDs never gain a `SERVICE` reference no matter how many objects they hold. They stay `Local` because the whole point of a CRD is that the kube-apiserver serves it \u2014 no server for you to run."},{text:"Aggregation is required any time a CRD needs to serve more than one version",correct:!1,explanation:"Multiple versions, the storage version, and conversion are all handled inside the ordinary CRD model. Needing several versions is not by itself a reason to move to an aggregated API server."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,r){r.d(t,{A:()=>n});var a=r(4848),o=r(6540);function s({q:e,state:t,onToggle:r}){return(0,a.jsxs)("div",{className:"question_QpiT",children:[(0,a.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,a.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,a.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let s=t.selected.has(o),n="option_qGol";return t.checked?e.correct?n+=" correct_LYA5":s&&(n+=" incorrect_kRFc"):s&&(n+=" chosen_H3WG"),(0,a.jsxs)("li",{children:[(0,a.jsxs)("button",{type:"button",className:n,disabled:t.checked,onClick:()=>r(o),children:[(0,a.jsx)("span",{className:"marker_vN_w",children:s?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(s||e.correct)&&e.explanation&&(0,a.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function n({questions:e}){let[t,r]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),i=t.every(e=>e.checked),c=t.filter((t,r)=>{var a,o;let s;return a=e[r],o=t.selected,s=a.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),a.multiSelect?o.size===s.length&&[...o].every(e=>a.options[e].correct):1===o.size&&s.includes([...o][0])}).length;return(0,a.jsxs)("div",{className:"quiz_I014",children:[e.map((o,n)=>(0,a.jsx)(s,{q:o,state:t[n],onToggle:t=>r(r=>r.map((r,a)=>{if(a!==n)return r;let o=new Set(r.selected);return e[n].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...r,selected:o}}))},n)),(0,a.jsxs)("div",{className:"actions_XUwt",children:[(0,a.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>r(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,a.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>r(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),i&&(0,a.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,r){r.d(t,{R:()=>n,x:()=>i});var a=r(6540);let o={},s=a.createContext(o);function n(e){let t=a.useContext(s);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function i(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:n(e.components),a.createElement(s.Provider,{value:t},e.children)}}}]);